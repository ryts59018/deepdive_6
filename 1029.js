const users = ["irangi","camel","ghost","sunny","henry"]
let userIndex = 3;

users.forEach(funciton (users,idx)) 
  if (idx === 2) {
console.log(`${idx+1}번쩨 user는 ${users}님 입니다.`)};

users.forEach((users,idx)=> {
    if (idx === userIndex - 1) {
console.log(`${userIndex+1}번쩨 user는 ${users}님 입니다.`);
    }

});
